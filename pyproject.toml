[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyNMMS"
dynamic = ["version"]
description = "An automated reasoner for the Non-Monotonic Multi-Succedent (NMMS) sequent calculus from Hlobil & Brandom 2025, Ch. 3."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Bradley P. Allen" },
]
keywords = ["logic", "sequent-calculus", "nonmonotonic", "reasoning", "inferentialism"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Mathematics",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov",
    "hypothesis>=6.0",
    "ruff",
    "mypy",
    "rdflib-endpoint>=0.5",
    "uvicorn>=0.30",
]
docs = [
    "mkdocs-material",
    "mkdocstrings[python]",
]
rdf = [
    "rdflib>=7.0",
    "owlrl>=7.0",
]

[project.scripts]
pynmms = "pynmms.cli.main:main"

[project.urls]
Homepage = "https://github.com/bradleypallen/pyNMMS"
Repository = "https://github.com/bradleypallen/pyNMMS"

[tool.hatch.version]
path = "src/pynmms/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["src/pynmms"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 99

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
